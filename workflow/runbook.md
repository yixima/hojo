# 週次実行手順（ランブック）

毎週1回、この手順を最初から最後まで実行する。
定期実行された新しいセッションは会話履歴を持たないため、**この文書だけを読めば
実行できる**ように書いてある。

## 前提の確認（最初に必ず行う）

```bash
curl -sS -o /dev/null -w "%{http_code}\n" --max-time 15 https://www.jgrants-portal.go.jp/
```

`000` が返る場合、クラウド環境のネットワークアクセスが `Trusted` のままで、
公的機関のサイトが全て遮断されている。この状態では公募要領を読めないため、
**WebSearch だけで判定できる範囲に留め、レポート冒頭にその旨を明記する。**

解消するには、claude.ai/code の環境セレクタ（メッセージ入力欄の上の雲アイコン）から
ネットワークアクセスを `Custom` にし、`workflow/sources.yaml` の
`network_allowlist` の7行を「許可されたドメイン」に登録する。
「一般的なパッケージマネージャーのデフォルトリストも含める」のチェックは必ずオンにする。

---

## 手順1：既報案件の読み込み（重複防止）

```bash
cat data/ledger.csv
```

このファイルが今までに報告した案件の一覧。**ここに載っている案件は再掲しない。**
ファイルが無ければ新規作成する（ヘッダは手順5を参照）。

同一案件かどうかは `発注機関 + 案件名` の組み合わせで判定する。年度が変われば
別案件として扱う（例：令和7年度版と令和8年度版は別）。

---

## 手順2：発見（WebSearch）

`workflow/keywords.yaml` の `query_templates` に沿ってクエリを組み立て、
**10〜15本の WebSearch** を実行する。年度の語（令和8年度 / 2026年度）を必ず含める。

クエリ例：
```
伝統的工芸品 海外 企画提案 公募 2026年度 締切
海外展示会 出展 運営業務 企画競争 令和8年度
地域産品 海外展開 補助金 公募 2026
インバウンド プロモーション 業務委託 プロポーザル 令和8年度
自治体 シティプロモーション 海外 公募型プロポーザル 2026
文化芸術 海外発信 公募 令和8年度
ジャパンパビリオン 運営 委託 公募
越境EC 販路開拓 補助金 2026年度 公募中
```

検索は表現を変えて複数回行う。同じ語だけを使うと同じ結果しか返らない。

---

## 手順3：優先サイトの直接確認

`workflow/sources.yaml` の `tier_a`（6件）は、検索でヒットしなくても**毎回必ず**
公募情報ページを直接開いて確認する。特に以下は御社の事業と直結している。

- 伝統的工芸品産業振興協会 公募公告
- 東京都中小企業振興公社（東京手仕事）

`tier_b` は月1回（第1週）に確認する。
`tier_c_municipalities` は年度初め（4〜6月）とその前後に重点確認する。

ネットワークが遮断されている場合、この手順は WebSearch での代替に留める。

---

## 手順4：判定

見つかった各案件について、`workflow/eligibility.md` の手順を適用する。

1. 足切り判定（500万円未満・締切超過・除外語・地域限定・個人限定）
2. 応募形態の判定（単独／共同／支援側／受託）
3. 全省庁統一資格の要否と間に合うか
4. スコアリング（S/A/B/C）
5. 実績要件を満たせるか

**判定の根拠となる記述は、公募要領の該当箇所を引用して残すこと。**
「応募できます」だけでは生島様が確認できない。

### 締切の扱い

| 締切までの残日数 | 表示 |
|---|---|
| 7日未満 | **【至急】** 準備期間がほぼない。提出可否の即断が必要 |
| 8〜21日 | **【要着手】** 通常の提案書作成期間 |
| 22日以上 | 通常 |

---

## 手順5：台帳への追記

`data/ledger.csv` に追記する。列は以下。

```csv
初報日,案件名,発注機関,種別,締切,予定価格,格付,応募形態,資格要否,状態,URL
```

- `種別`: 補助金 / 企画競争 / 企画提案 / プロポーザル
- `応募形態`: 単独 / 共同 / 支援側 / 受託
- `資格要否`: 不要 / 要（間に合う）/ 要（間に合わない）
- `状態`: 新規 / 検討中 / 提出済 / 見送り / 締切超過（初報時は「新規」）

追記したらコミットしてプッシュする。

```bash
git add data/ledger.csv && git commit -m "週次巡回 YYYY-MM-DD" && git push
```

---

## 手順6：レポート生成と送信

`workflow/report-template.md` の形式でレポートを作る。

1. **Gmail で送信** — 宛先 iximay@gmail.com、件名は
   `【公募レポート】YYYY年M月D日 新規N件（うちS格M件）`
2. **Google Drive に蓄積** — `全国統一入札資格_2026` フォルダ
   （ID: `1jMRvEndX0NBNj2Ad5X4nrAgT5nm2fMGs`）に
   `kobo_report_YYYYMMDD.md` として保存する。命名規則は Drive の README に従う。

### 新規案件が0件だった場合

**メールは送る。** 「今週は該当なし」と分かることに価値がある。ただし本文は簡潔にし、
確認したソース数と、締切が近づいている既報案件のリマインドだけを載せる。

---

## 手順7：既報案件の締切チェック

`data/ledger.csv` の `状態` が `新規` または `検討中` の案件について、
締切が過ぎていれば `締切超過` に更新する。締切が7日以内に迫っているものは
レポートの冒頭でリマインドする。

---

## 実行後の確認

- [ ] `data/ledger.csv` を更新してプッシュしたか
- [ ] Gmail を送信したか
- [ ] Drive にレポートを保存したか
- [ ] ネットワーク制限があった場合、その旨をレポートに明記したか
